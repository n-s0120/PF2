<div class="article-edit">
  <div class="article-edit-title">
    <h1>記事編集画面</h1>
  </div>

  <div class="article-edit-content">
    <%= form_with model: @article, local: true do |f| %>
      <div class="form-group">
        <%= f.label :記事タイトル %>
        <br>
        <%= f.text_field :title %>
      </div>

      <div class="form-group">
        <%= f.label :ページアドレス %>
        <br>
        <%= f.text_field :url %>
      </div>

      <div class="form-group">
        <%= f.label :記事紹介 %>
        <br>
        <%= f.text_area :introduction %>
      </div>

      <div class="form-group">
        <%= f.label :タグ %>　（※タグを複数つけたい場合は、「,」で区切ってください。）
        <br>
        <!--join(',')の指定をして、「,」で区切ればタグになるように指定-->
        <%= f.text_field :tag_list, value: @article.tag_list.join(',') %>
      </div>

      <div class="form-group">
        <%= f.submit "編集する", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>